import React from "react";
import { Button } from "react-bootstrap";

const ButtonGenerik = ({ children, variant = "primary", ...props }) => {
  return (
 <Button variant={variant} {...props}>
      {children}
    </Button>
  );
};

export default ButtonGenerik;